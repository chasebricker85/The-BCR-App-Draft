# Dependencies for the Streamlit app
(BCR.py).
# Streamlit Community Cloud reads this file and installs these before it runs
# the shell_bcr_full_three.py needs nothing beyond the Python standard library. 
# So it is not listed here.
#
# These three are a known-good, compatible set that supports the app as written. 
# If you would rather match the exact versions you run locally, find them with: python -c "import streamlit,pandas,plotly;print(streamlit.__version__,pandas.__version__,plotly.__version__)"
# and edit the numbers below to match.
streamlit==1.39.0
pandas==2.2.3
plotly==5.24.1
